{
  "name": "app",
  "module": "index.ts",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "start": "bun run ./src/index.ts",
    "build": "rm -rf ./dist && mkdir ./dist && bun build ./src/index.ts --compile --minify --outfile ./dist/rbclm"
  },
  "type": "module",
  "dependencies": {
    "@inquirer/prompts": "^3.2.0",
    "chalk": "^5.3.0",
    "csv-parse": "^5.5.0"
  }
}